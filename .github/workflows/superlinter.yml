on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
        
      - name: Set up openJDK
        uses: actions/setup-java@v1
        with:
          java-version: '9'
      - name: Build with Maven
        run: make build
